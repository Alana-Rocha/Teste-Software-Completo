const duplicada1 = (situacao: number) => {
  if (situacao > 5 && situacao < 7) {
    return true;
  } else {
    return false;
  }
};

const duplicada2 = (situacao: number) => {
  if (situacao > 5 && situacao < 7) {
    return true;
  } else {
    return false;
  }
};
